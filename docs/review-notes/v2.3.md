# X Articles: Real Source Before Connections

This pass fixes the Chamath `What Is a Software Factory?` acceptance save: Hilt had the wrapper tweet but not the article, yet checked off Connections after weaving around 138 characters of metadata.

Review that X Articles now use their real X title and long-form body, produce a source-grounded digest, and update the same card/reader. A missing article field should stop at `Needs source` without running connections. The remaining fallback is authenticated browser recovery when X does not return `article.plain_text`.

Full history: `docs/PIPELINE-VERSIONS.md`.
